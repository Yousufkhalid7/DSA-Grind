274. H-Index
Approach: Sorting and Traversing

Solution:
class Solution(object):
    def hIndex(self, citations):
        citations.sort()
        for i in range(len(citations)):
            if citations[i] >= (len(citations) - i):
                return len(citations) - i
        return 0